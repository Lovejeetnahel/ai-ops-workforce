import { ModuleStub } from '../../../components/ModuleStub';

export default function PaymentsPage() {
  return (
    <ModuleStub
      icon="▭"
      title="Payments"
      phase="Phase 12 of the implementation roadmap"
      description="Invoices, estimates, payment links, subscriptions and Documents — contracts, proposals and e-signatures — all in one place, built on the existing Stripe-backed revenue engine."
    />
  );
}
