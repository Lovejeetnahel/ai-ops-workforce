import { ModuleStub } from '../../../components/ModuleStub';

export default function AppsPage() {
  return (
    <ModuleStub
      icon="▦"
      title="Apps"
      phase="Phase 13 of the implementation roadmap"
      description="Optional capabilities most businesses never need: Field Operations, Inventory, Fleet and HR. Enable only what your business uses — everyone else never sees them."
    />
  );
}
