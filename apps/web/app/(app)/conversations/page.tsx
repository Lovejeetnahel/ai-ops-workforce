import { ModuleStub } from '../../../components/ModuleStub';

export default function ConversationsPage() {
  return (
    <ModuleStub
      icon="▤"
      title="Conversations"
      phase="Phase 5 of the implementation roadmap"
      description="One inbox for SMS, email, chat, social DMs and calls — with assignment, internal notes and AI-drafted replies. The existing inbox is being ported and unified here."
    />
  );
}
