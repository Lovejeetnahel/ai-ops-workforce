import { ModuleStub } from '../../../components/ModuleStub';

export default function SalesPage() {
  return (
    <ModuleStub
      icon="▲"
      title="Sales"
      phase="Phase 4 of the implementation roadmap"
      description="Multiple pipelines, opportunities and follow-up tasks. The existing pipeline board is being ported and extended here with multi-pipeline support."
    />
  );
}
