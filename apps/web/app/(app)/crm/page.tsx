import { ModuleStub } from '../../../components/ModuleStub';

export default function CrmPage() {
  return (
    <ModuleStub
      icon="◈"
      title="CRM"
      phase="Phase 3 of the implementation roadmap"
      description="Contacts, Companies, tags, smart lists, notes and activity timelines — the relationship layer every other module reads from. Landing here next."
    />
  );
}
