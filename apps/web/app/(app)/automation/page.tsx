import { ModuleStub } from '../../../components/ModuleStub';

export default function AutomationPage() {
  return (
    <ModuleStub
      icon="⟳"
      title="Automation"
      phase="Phase 11 of the implementation roadmap"
      description="Recipes and a visual branching Workflows canvas, wired to the real rule engine and workflow graph interpreter already running in the backend — plus a template library and execution history."
    />
  );
}
